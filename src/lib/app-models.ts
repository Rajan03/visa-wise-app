const domainCollection = {
    collectionName: "domains",
    subcollections: {
        users: {
            name: "users",
        },
    },
}

export const AppCollections = {
    domain: domainCollection,
}